/*const sum = require("./script");

describe("sum module", () => {
  test("name of test", () => {
    expect(sum(3, 4)).toBe(7);
  });
  test("negative of test", () => {
    expect(sum(-3, 4)).toBe(2);
  });
});

describe("sum negative", () => {
  test("name of test", () => {
    expect(sum(0, 6)).toBe(6);
  });
  test("negative of test", () => {
    expect(sum(5, 6)).toBe(12);
  });
});*/
